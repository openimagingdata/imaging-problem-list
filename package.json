{
  "dependencies": {
    "playwright": "^1.56.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "deploy": "wrangler pages deploy viewer --project-name=imaging-problem-list --branch=main"
  }
}
